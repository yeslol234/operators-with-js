//This is an operator with an variable.Since the variable "e" is not defined it does not work.
e - 2
//This is what defines the variable "e".
let e;
e=32
//It is now defined but copy and pasting this code does not work because it was after the "e-2" so it was defined after the operation.
